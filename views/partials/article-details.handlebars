{{!-- Article Card --}}
<div id="article-card">
    {{!-- Card Header --}}
    <div id="card-header">
        <h2>{{title}}</h2>
        <p>Posted by {{user_id}} on {{date_created}}</p>
    </div>
    {{!-- Card Body --}}
    <div id="card-body">
        <p>{{body}}</p>
    </div>
</div>